apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "net.fullname" . }}-test-connection"
  labels:
    {{- include "net.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "net.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
